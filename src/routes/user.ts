import express from 'express';
// import UserController from '../controllers/user';
// import authenticate from '../middlewares/authenticate';
// import validate from '../middlewares/validate';
// import { createUserInsightSchema, updateUserInsightSchema } from '../schemas/insight';
// import multer from 'multer';
// import { UserRoles } from '../config/constants';

const router = express.Router();

// const storage = multer.memoryStorage();
// const upload = multer({ storage });

// router.get('/writers', UserController.getWriters);
// router.post('/writers/request', authenticate.user, UserController.createWriterRequest);
// router.get('/writers/request', authenticate.user, UserController.getWriterRequest);
// router.get('/:username', UserController.getUser);
export default router;
